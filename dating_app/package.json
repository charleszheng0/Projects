{
    "name": "dating_app_root",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server",
        "web"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
        "install:all": "npm install"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}