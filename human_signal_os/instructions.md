Build me a Next.js app with TailwindCSS. It should access the user's webcam and display the live feed on a canvas element. On top of the canvas, use MediaPipe FaceMesh (loaded from CDN, not npm) to draw 468 facial landmarks as small dots in real time. The dots should change color from green to red based on a confidenceScore variable between 0 and 1 that I'll wire up later. Add a sidebar panel on the right that shows placeholder values for: Confidence Score (0-100), Bias Flags (empty list), and Dominant Emotion (Neutral). Make it look like a dark-mode analytics dashboard, not a toy. Ensure that the entire project stays within the folder human_signal_os. 