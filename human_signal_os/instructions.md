Build me a Next.js app with TailwindCSS. It should access the user's webcam and display the live feed on a canvas element. On top of the canvas, use MediaPipe FaceMesh (loaded from CDN, not npm) to draw 468 facial landmarks as small dots in real time. The dots should change color from green to red based on a confidenceScore variable between 0 and 1 that I'll wire up later. Add a sidebar panel on the right that shows placeholder values for: Confidence Score (0-100), Bias Flags (empty list), and Dominant Emotion (Neutral). Make it look like a dark-mode analytics dashboard, not a toy. Ensure that the entire project stays within the folder human_signal_os. 
Redesign the Inflect Live Tracker UI inspired by Whoop's design language. Use a true black background (#000000) with dark card surfaces (#111111 or #1A1A1A) and a single accent color in Whoop's signature red-orange (#FF4757) reserved only for active scores and alerts — everything else in neutral whites and grays. Typography should be clean and athletic: bold heavy numerics for scores and metrics, light-weight uppercase tracked labels in muted gray (#888888) for categories. The signal score should be a large, confident circular arc gauge — thick stroke, glowing accent fill, score number prominent and centered. Progress bars should be sleek and pill-shaped, filled in white or accent color against a dark track. The face tracking overlay should be reduced to a barely-visible single-color contour mesh — dark green or white at 25% opacity — so it reads as technical but unobtrusive. Cards should have very subtle inner borders (#2A2A2A) with soft corner radii. No gradients, no decorative elements, no competing colors. The overall feel should be performance-focused and premium — like a serious athlete's dashboard. Data is the hero, the interface disappears.